#include "main.h"
#include <stdio.h>

/**
 *_puts - put a string
 *@str: A pointer to an integer that will be changed
 *
 *Return: void which means our answer is correct
 */
void _puts(char *str);
({
	while (*str != '\0')
	{
		_putchar(*str + 0);
		str++
	}
	_putchar('\n');
})
