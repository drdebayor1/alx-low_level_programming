#include "main.h"
/**
 * factorial - factorial of a given number
 * @a: pointer block of memory to fill
 * Return: factorial
 */

int factorial(int a)
{
	if (n == 0)
		return (1);

	if (n < 0)
		return (-1);

	else 
		return (n * factorial(n - 1));

}
